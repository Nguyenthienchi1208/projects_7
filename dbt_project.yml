
name: 'dec_project2'
version: '1.0.0'
config-version: 2


profile: 'dec_project2'


model-paths: ["models"]
test-paths: ["tests"]
target-path: "target"
log-path: "logs"
packages-install-path: "dbt_packages"

clean-targets:
  - "target"
  - "dbt_packages"

models:
  dec_project2:
    staging:
      +materialized: view
      fact:
        +materialized: table
    analytics:
      +materialized: table
      +schema: glamira_dwh